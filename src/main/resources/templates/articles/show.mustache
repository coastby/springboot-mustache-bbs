{{>layouts/header}}
{{#article}}
<div class="container mt-5">
<div class="card">
    <div class="card-header">
        {{id}}. {{title}}
    </div>
    <div class="card-body">
        {{content}}
    </div>
</div>
{{/article}}

    </div>

{{>layouts/footer}}